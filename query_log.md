```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Name, State, AVG(Donations) as avg_donations FROM DemCandidatesDB GROUP BY Name, State ORDER BY avg_donations DESC LIMIT 10
```

```response from databricks
[]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

```sql
SELECT Performer, Show, 
            MIN(Show_Start) AS Earliest_Show_Start,MAX(Show_End) AS Latest_Show_End
            FROM showdataDB
            GROUP BY Performer, Show
            ORDER BY Earliest_Show_Start DESC
            LIMIT 10
            
```

```response from databricks
[Row(Performer='      <h1 id="search-suggestions-dialog-header" class="sr-only">Search code', Show=' repositories', Earliest_Show_Start=' users', Latest_Show_End=' issues')]
```

